{
  "buildCommand": "npx prisma generate && cross-env NODE_ENV=production NODE_OPTIONS=--no-warnings NX_NO_CLOUD=true NX_CACHE_DIRECTORY=/tmp/nx-cache NX_WORKSPACE_DATA_DIRECTORY=/tmp/nx-data npx nx build administration",
  "outputDirectory": "web/apps/administration/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
